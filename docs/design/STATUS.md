# STATUS — where everything is, what actually works

Single page. If it isn't here, it isn't real. Updated 2026-09-03 (night) — hardening complete; live-engine e2e passed; branch testable via `docs/TESTING.md`; navigation (`summer open`), the runtime librarian (`summer_search_library` / `summer_read_library` with the feedback footer), and an enforced tag vocabulary all landed. Later the same day the eight scene-scripting wave branches were merged into one reviewable integration branch (counts below reflect it; the merge record is `SCENE_SCRIPTING_INTEGRATION.md`). Findings ledger: REVIEW-2026-09-02.md. Companion: ROADMAP.md (sequencing), CONTRACT.md (rules).

## The one place
**`SummerEngine/summer-engine-agent` branch `v3-foundation`** (PR #18 open, NOT for merging yet). Everything toolkit-side lives here. Nothing has shipped: origin `main` and npm `summer-engine@2.8.2` are untouched. The scene-scripting waves (events, fabrication, perception, editor UI, runtime control, outcome evals, skill syncs) sit on top of it as `scene-scripting/v3-integration`, one merge commit per wave branch, pending review.

## What is verified working (how)
All rows re-verified after the 2026-09-02 fix wave; agent self-reports were NOT trusted — an HEAD presence audit + full gate run confirmed each.
| Thing | Verified by |
|---|---|
| Registry compiler → all root manifests, index, counts, aliases | `generate:registry --check` no drift; 23 compiler tests; CI parity step |
| Library: 209 resources validate (86 tools / 95 skills / 19 templates / 9 references — navigation entries added 2026-09-03, scene-scripting waves integrated the same day), capability lint clean (documented exceptions only), `remote` explicit on every tool, authority fields audited against implementations. 14 skills are `preview` (7 = source-cited intake from Marcus's gamedev-knowledge pipeline, 7 depend on unmerged engine ops); `summer setup` installs them labelled by default, `--stable-only` skips | `validate:library` + resource.yaml audit |
| `summer` CLI boots; `summer tool --list` (86); `summer skills list` (95); unknown commands exit 1; `summer open`/`run`/`install` safe paths | `npm run build` + smoke + 1271 tests (9 skip without an engine build) |
| `summer_search_library` (BM25 + optional semantic fusion, lexical-only offline, never throws) and `summer_read_library` (full entry + feedback footer `entry_id@hash` — the reporting trigger) on both faces, engine-free | 64 tests; live probes: "make stylized water" → vfx-water-ripple #1, "take a screenshot of the game" → tool/screenshot #1 |
| `summer open <target>` / `summer_open`: intent → exact web page (login-gated via `/login?returnUrl=`) or editor surface (OpenScene/SelectNode/OpenResource/FocusDock/RevealInFileSystem); product map = code with a drift test; 11 editor targets honestly `planned` with the engine op named | 55 tests + scratch-HOME CLI smoke (`--print`); live editor targets pending an open project |
| 39 stable engine tools + 22 engine-free tools + 25 gated (preview) tools (the split `summer tool --list` shows; incl. `summer_starcast` from engine PR #147; `summer_frame_camera` + `summer_camera_visibility` removed 2026-09-03 — Marcus dropped them after benchmarks; the 15 scene-scripting wave tools added 2026-09-03 are all preview); one behavior per tool, CLI + MCP faces share the same functions; a preview tool on an old engine returns a structured `engine_lacks_op` (events tools: `engine_lacks_events`) result on BOTH faces | descriptor↔zod parity + mirror-parity tests; **live e2e on engine 0.5.65 with a real project (2026-09-02): project context, scene tree, viewport screenshot all worked through `summer tool`** — the wave tools have unit + parity coverage only, no live engine yet |
| Testable on this branch without publishing: `summer setup <agent> --local-dev` points the agent at this checkout's build; recipe in `docs/TESTING.md` (every command executed) | fake-HOME setup run + gate run |
| `summer_library_feedback` → live endpoint → Supabase table → /admin view | 29 MCP tests + 25 route tests; loopback wire proof; table verified live |
| Headless routing layer | 58 unit tests + fake worker; real-binary test SKIPS (no engine build) |
| Routing eval — tuning set (91 q): recall@5 1.0 / recall@1 0.81 / MRR 0.90. **Held-out set (42 q, written blind): recall@5 0.84 / recall@1 ~0.53 / MRR ~0.73** — up from 0.79/0.50/0.70 after the metadata wave, without touching the held-out queries | `npm run eval:routing` (gated) + `eval:routing:heldout` (report-only). Held-out is the real number. |
| Template pinning: `summer create` fetches the pinned SHA, verifies tree digest, refuses on mismatch, writes `.summer/project.json` | 84 tests + live fetch of a real template repo (digest matched) |
| Descriptor ↔ zod parity gate; validator cross-checks descriptors vs real registrations, implementation paths, schema shape | `npm test` — found and fixed 3 drifts on first run |
| Hooks fire in Claude Code/Cursor (matcher/if per docs); OpenCode plugin loads the library; `setup` installs all 83 skills incl. `using-summer` | hook smoke + opencode smoke test + cold install in fake HOME |
| **Navigation (2026-09-03):** `tool/open` = `summer_open` MCP + `summer open <target>` CLI (+ `summer tool open`), one implementation (`src/core/capabilities/navigation/`); `reference/product-map` (64 destinations: 45 web verified against the web repo's route files, 8 editor ops verified in `op_registry.json`, 11 editor targets `planned` with the engine op named); `skill/navigate-summer`. Web targets deep-link through `/login?returnUrl=`; engine-off and planned targets return structured `ok:false` results and open nothing. `summer open <project-dir>` unchanged. Design: `NAVIGATION-DESIGN.md`, research: `NAVIGATION-RESEARCH.md` | 55 unit tests (resolution, ambiguity, `--print`, not-logged-in, engine-off, planned, product-map ↔ targets parity) + descriptor↔zod parity + dispatch mirror test + CLI smoke under a scratch HOME (`--print`/`--list`/engine-off/ambiguous/legacy path). **Live-engine open of a scene/node NOT yet exercised** (no editor with the API up during the build). |

## What exists as code but CANNOT work yet (engine ops unmerged)
25 tools gated on unmerged engine features degrade with `engine_lacks_op` — the two events tools with `engine_lacks_events` — (override: `SUMMER_CAPABILITY_PREFLIGHT=off`):
scripting (run_script, world_snapshot, snapshot_diff, get_runtime_tree, inspect_runtime_node), spatial (4 of #158's six ops — frame_camera and camera_visibility dropped) + starcast, and the scene-scripting waves integrated 2026-09-03 — events channel (wait_for_event, recent_events, `summer events`), mesh fabrication (fabricate_3d), camera bookmarks (camera_bookmark; fixed-pose `summer_screenshot` framings), editor UI control (ui_actions, ui_tree, ui_activate, ui_screenshot), runtime control (game_probe, game_control, game_input, runtime_set, runtime_call, runtime_spawn, runtime_animate; the instance/mode/deterministic parameters of play and stop) — plus headless routing (flag `SUMMER_HEADLESS_ROUTING=1`) which needs the worker. `run_editor_script` ships today (corrected below).
Unblocked by: **SummerEngine/SummerEngine PR #155 (headless worker) + #156 (scene scripting) and the #156 follow-up commits on the engine's scene-scripting branch (waves I/K/L + the events channel) + #158 (Marcus's six-op spatial suite, with companion fixes) + #147 (starcast, Marcus's own PR)** — all rebase clean onto main; #147 and #158 conflict with each other in 13 files (whichever lands second re-rebases + regenerates the op registry). **None built by us.** Owners must build + smoke + merge.

## NOT yet done (honest)
- Cold-machine install test: RUN (tarball → fake HOME → setup → MCP handshake → 68 tools at the time, 86 today → engine-less calls). Mechanics passed; the P0s it found (doctor short-circuit, stale skill refs) are fixed; live-engine step still unverified (running editor exposed no API during the test).
- Independent adversarial review: RUN (6 reviewers). Findings + owners in REVIEW-2026-09-02.md; P0/P1 fixed; P2 debt scheduled (consolidation pass).
- Skill bodies: v2 references purged (359 invocations, 46 links, 20 dead targets); bare-slug cross-reference convention documented in docs/DEVELOPMENT.md.
- Held-out routing gap: 1.0 vs 0.84 — narrowing via metadata quality; 161 one-way `related` links surfaced as WARN (content backlog).
- `feat/templates-mcp-tool` dirty checkout (someone's uncommitted `with-engine.ts` + `summer_list_templates` work) — unreconciled with v3's `with-engine.ts` changes.
- Count guards now derive from counts.json (no literals).
- CONTRACT/AGENTS/README/CHANGELOG now match the code (truth pass 2026-09-02); planned-not-implemented is labelled as such: `.summer/state.json`/`decisions.ndjson`/`receipts/`, runtime alias resolution for skills/tools (aliases.json is generated, only template aliases resolve), Tier-1 1500-char feedback notes, Summer-aware side-loading. Folding the CLI/MCP mirror into shared capabilities: helpers deduped (one copy each, parity-tested); registration still happens twice by design for now.
- `evals/canary/` — Marcus's blind A/B tool-canary gateway, runnable (`npm run eval:canary`), records evidence, does not score yet.
- **Scripting today:** `summer_run_editor_script` (RunEditorScript) and `RunVerification` probes SHIP in engine 0.5.65 — agents can already run editor-side GDScript and runtime probe scripts. The checkpoint/rollback `summer_run_script` (RunSceneScript) + world snapshots need PR #156. (Corrected 2026-09-03: run-editor-script had been mislabelled as gated.)
- Live-engine verification of the 25 preview tools: impossible until engine PRs #155/#156 and the #156 follow-up commits merge (the engine-free tools and the 39 stable engine tools were live-verified on 0.5.65; the 15 wave tools added 2026-09-03 have never seen a live engine from this package — their op names are checked against the engine's feature-branch `op_registry.json` by `src/core/op-registry-drift.test.ts`, where the honesty half is EXPECTED to fail until the waivers are removed).
- `summer tool library-feedback` passes `reports` through without the MCP face's zod validation (invalid outcome → gateway `dropped:true` instead of a local error) — small, tracked.
- Capability lint does not flag `git clone` / `cmake --install` / `hf download` as install commands — decide whether it should.
- P2 leftovers: 3 "Summercraft" strings in src/core/auth.ts + login.ts; embeddings sidecar not shipped yet (semantic search activates when `generate:registry --embed` runs with a provider and `/api/mcp/embed` exists on the gateway — web PR).

## Other places (dependencies, not homes)
- Web repo branch `feat/library-feedback-mailbox` (endpoint + admin; pushed, no PR). Salt env set in Vercel. RLS fix applied.
- Web cloud cleanup: task chip pending (not started).
- Engine PRs #155 / #156 (above). Engine local `main` has 5 stale unpushed commits — abandon.

## Ship gates (all Mathias)
PR #18 → main **together with** web mailbox branch **and** npm publish 3.0.0; then rename repo → `summer`, org → `summerengine`; then web copy pass.
