id: skill/create-asset-sheet
kind: skill
version: 1.0.2
summary: Generate a pack of 2D game assets from one prompt — tile sheet, UI kit, character pack, or biome set — sliced, classified, and saved as one pack ArtAsset.
use_when:
  - generating a pack of 2D game assets from a single prompt — a tile sheet, UI kit, menu screen, character pack, or biome set
  - a 2D game needs many matching sprites, tiles, or UI pieces in one consistent style rather than one image at a time
facets:
  lifecycle:
    - build
  domains:
    - 2d
    - assets
  modalities:
    - images
    - assets
compatibility:
  engine: ">=4.6"
related:
  skills:
    - skill/character-portrait
    - skill/concept-art
    - skill/pixel-art
    - skill/sprite-sheet
    - skill/tileable-texture
    - skill/asset-strategy
    - skill/design-level
source: official
license: MIT
status: stable
recommended: false
aliases:
  - skills/2d-assets/create-asset-sheet
