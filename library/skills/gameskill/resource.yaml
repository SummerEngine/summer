id: skill/gameskill
kind: skill
version: 1.0.1
summary: Capture what a game-dev session just learned into Summer's canonical skill library so the next session and every Summer user start smarter.
use_when:
  - finishing a game-development session with learnings worth capturing into Summer's canonical skill library
  - a session discovered a technique, workaround, or convention that future sessions and other Summer users should reuse
facets:
  lifecycle:
    - build
  domains:
    - agent-workflow
    - meta
  modalities:
    - docs
compatibility:
  engine: ">=4.6"
source: official
license: MIT
status: stable
aliases:
  - skills/workflow/gameskill
