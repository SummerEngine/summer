id: tool/instantiate-scene
kind: tool
version: 1.0.0
summary: Add an existing scene or 3D model file as a child node, composing scenes from prefabs.
use_when:
  - placing a .tscn prefab or imported 3D model into a scene
facets:
  lifecycle:
    - build
  domains:
    - scene
compatibility:
  engine: ">=4.6"
source: official
license: MIT
status: stable
aliases:
  - summer_instantiate_scene
implementation:
  module: src/mcp/tools/scene-tools.ts
  export: registerSceneTools
surfaces:
  mcp:
    tool_name: summer_instantiate_scene
input_schema:
  type: object
  properties:
    scenePath:
      type: string
      description: Target scene to receive the instance, e.g. 'res://main.tscn'
    parent:
      type: string
      description: Parent node path, e.g. './World'
    scene:
      type: string
      description: Scene/model path, e.g. 'res://player.tscn' or 'res://models/tree.glb'
    name:
      type: string
      description: Override the instance name
  required:
    - scenePath
    - parent
    - scene
  additionalProperties: false
authority:
  filesystem: true
  editor_mutation: true
  network: false
  credentials: false
  publish: false
evidence_checks:
  - scene-valid
