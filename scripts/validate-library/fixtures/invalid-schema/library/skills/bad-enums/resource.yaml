id: skill/bad-enums
kind: skill
version: 1.0.0
summary: A skill with invalid enum values.
use_when:
  - testing schema violations
facets:
  lifecycle: [shipit]
source: official
license: MIT
status: beta
